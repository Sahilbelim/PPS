// To find area of square(a=l*l) where a = area, l = length
#include<stdio.h>
void main()
{
    int a, l;
    printf("Enter Length of Square:");
    scanf("%d", &l);
    a = l * l;
    printf("\n Area of Square: %d", a);
}